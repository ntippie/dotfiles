[profile red-tiger]
region=us-west-2
output=json
credential_process = aws-vault export --format=json red-tiger

[default]
region=us-west-2
output=json

[profile main-admin]
region=us-east-1
output=json
credential_process = aws-vault export --format=json --no-session main-admin
